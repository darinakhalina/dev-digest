# docs — reviewer-core

Deep-dives that do not fit `README.md`: longer explanations, decision notes, diagrams.
`README.md` stays the source of truth for the pipeline and the public API — link to it here, do not
copy it.

One topic per file, named `NN-kebab-topic.md`. If an agent should read a doc for a whole class of
tasks, add a line to `../AGENTS.md` § *Use when*.
